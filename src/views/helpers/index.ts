import forViewHelper from './forViewHelper';
import debugViewHelper from './debugViewHelper';
import formFloatingInputHelper from './formFloatingInputHelper';
import flashRenderHelper from './flashRenderHelper';

export default {
    formFloatingInput: formFloatingInputHelper,
    for: forViewHelper,
    debug: debugViewHelper,
    floatingInput: formFloatingInputHelper,
    flashRender: flashRenderHelper,
};
